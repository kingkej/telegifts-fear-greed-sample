# GiftAsset API credentials
# Apply for an API key at GiftAsset.
# Do NOT commit your real .env file.

# Base URL must point at giftasset.pro (per workshop sample requirements)
VITE_GIFTASSET_BASE_URL="https://api.giftasset.pro"

# API key header
VITE_GIFTASSET_API_KEY="YOUR_GIFTASSET_API_KEY"

# Optional: if GiftAsset requires Bearer auth, paste a generated JWT/token here
# VITE_GIFTASSET_BEARER_TOKEN="YOUR_BEARER_TOKEN"
